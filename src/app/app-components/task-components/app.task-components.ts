import { Component, OnInit } from '@angular/core';
import { ActivatedRoute } from '@angular/router';
import { Router } from '@angular/router';

@Component({
    selector: 'task-page',
    moduleId: module.id,
    templateUrl: './app.task-components.html'
})
export class TaskComponent {

}